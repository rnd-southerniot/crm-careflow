-- Optional init script for Postgres container.
-- Note: scripts in /docker-entrypoint-initdb.d run only on first init (empty data dir).
-- Keep this file safe/idempotent.

-- Example extensions (uncomment if needed):
-- CREATE EXTENSION IF NOT EXISTS pgcrypto;
